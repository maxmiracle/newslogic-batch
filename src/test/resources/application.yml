spring:
  batch:
    job:
      enabled: false
  shell:
    interactive:
      enabled: false

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:

  data:
    mongodb:
      host: localhost
      port: 0
      database: newsChartsDb

app:
  start-date: 2020-01-01
  end-date: 2020-01-02

retry:
  maxAttempts: 3
  maxDelay: 10000